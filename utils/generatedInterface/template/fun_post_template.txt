    def _fun_name(selffunctions):
        if self.user is None:
            data = {fun_params}
        else:
            data = {'_tk_': self.user.token, '_deviceId_': self.user.device_idfunc_param}
        response = self.request.post(url=self.url+'key', data=data, hosts=self.url)
        apiTestResult(api='key', host=self.url,datas=data, resp=response)
        return self.__judge_response_status(json.loads(response))
